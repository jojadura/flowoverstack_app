<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <div id="questions">
	        	<h1>Preguntas</h1>
	        </div>	
	        <div id="btn-questions">
				    <% if signed_in? %>
      	    		<%= link_to "Hacer una Pregunta", new_question_path, class: "btn btn-primary" %>
      	  		<% else %>
      	    		<%= link_to "Hacer una Pregunta", new_user_session_path, class: "btn btn-primary" %>
    	  		<% end %>	        	
      	  </div>
    </div>

    <table class="table table-responsive">
      <thead>
        <th class="col-sm-1"></th>
        <th class="col-sm-1"></th>
        <th class="col-sm-10"></th>
      </thead>
        <% @questions.each do |question| %>
          <tr>
            <td class="text-center"><h3>0</h3>Votos</td>
            <td class="text-center"><h3>0</h3>Respuestas</td>
            <td class="text-left">
              <div id="body-questions">
                <h4><%= link_to question.title, question %> </h4>
                <span>Preguntó <%= question.user.email %> hace alrededor de <%= time_ago_in_words question.created_at %> </span>
              </div>              
            </td>
            </div>
          </tr>
        <% end %>
      </table>
 
        </div> <!-- .col-->
    </div> <!-- .row-->
</div> <!-- .container -->
